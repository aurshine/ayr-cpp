#pragma once
#include "container.hpp"

namespace ayr
{
	/*template<class T>
	class Sequence : public Container<T>
	{
	public:
		T& operator[](size_t index) = 0;

		const T& operator[](size_t index) const = 0;
	};*/
}
